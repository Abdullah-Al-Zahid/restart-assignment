1) undefined means a variable has been declared but not assigned a value by js....
null means an intentional absence of value, set manually by programmer.

2)map() is used to change each item in an array and make a new array.
forEach() is used to go through each item, but it doesn't make a new array.

3)== checks only the value
=== checks both value and type

4)async/await is used to fetch API data in a simple way also it help use data in html file.

5)Global Scope : A variable declared outside any function 
Function Scope : A variable declared inside a function also it can't work out side the declared fucntion.
